
  
  function initMap() {
    //map location
    var center = {lat: 9.75, lng: 79.9833 };
    
    var options = {
        zoom:8,
        center:center
    };
    //Create a new map
    var map = new google.maps.Map(document.getElementById("map"), options);
  };
  r
